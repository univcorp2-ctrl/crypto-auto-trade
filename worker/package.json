{
  "name": "crypto-auto-trade-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker port of the Crypto Auto Trade dashboard + engine.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250601.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "wrangler": "^4.0.0"
  }
}
